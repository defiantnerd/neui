#pragma once
// Tree<T> has moved to hosts/shared/tree.h.
// This header re-exports it under the original win32_host namespace so that
// existing win32 host source files need no changes.

#include "../shared/tree.h"
