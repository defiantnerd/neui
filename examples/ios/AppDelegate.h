#import <UIKit/UIKit.h>

// Minimal application delegate. All UI lives on the scene (SceneDelegate);
// the app delegate only routes scene-session configuration.
@interface AppDelegate : UIResponder <UIApplicationDelegate>
@end
